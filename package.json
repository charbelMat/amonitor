{
  "name": "node-monitor-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "node-monitor: error tracking, performance, uptime and alerting platform for Node.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "api:dev": "npm run start:dev --workspace=@node-monitor/api",
    "dashboard:dev": "npm run dev --workspace=@node-monitor/dashboard",
    "sdk:build": "npm run build --workspace=amonitor",
    "example": "node examples/express-app/server.js",
    "test": "npm run test --workspace=@node-monitor/api && npm run test --workspace=@node-monitor/dashboard && npm run test --workspace=amonitor",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  }
}
